<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_network_error"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorNetworkErrorBackground"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/ic_no_wifi"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center_vertical"
        android:padding="30dp"
        android:layout_weight="1"
        android:src="@drawable/ic_signal_wifi_off_black"/>

    <Button
        android:id="@+id/btn_try_to_reconnect"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="40dp"
        android:padding="20dp"
        android:text="@string/btn_try_to_reconnect"/>

</LinearLayout>